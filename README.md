# Microblog
Microblog flask app

To run in debug mode, enter in the terminal: 
```bash
FLASK_APP=microblog.py FLASK_DEBUG=1 python -m flask run
```

To install packages in virtual envirnment, enter in the terminal:
```bash
pip install -r requirements.txt
```
